# cid-workflow-version: 0.5.0

# This file is generated by the CID Workflow GitHub App.
# DO NOT EDIT!

# name
name: CID - DefaultBranch

# triggers
on:
  workflow_dispatch:
    inputs:
      loglevel:
        description: Log level
        required: true
        default: info
        type: choice
        options:
          - debug
          - info
          - warn
          - error
  push:
    branches:
      - main
    tags:
      - v*.*.*
    paths-ignore:
      - README.md
      - LICENSE
      - .gitignore
      - .editorconfig
      - renovate.json

# permissions, see https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#permissions and https://docs.github.com/en/rest/overview/permissions-required-for-github-apps
permissions:
  actions: read # detection of GitHub Actions environment
  checks: none
  contents: read
  deployments: none
  id-token: none
  issues: none
  packages: none
  pages: none
  pull-requests: none
  repository-projects: none
  security-events: none
  statuses: none

# cancel in progress when a new run starts
concurrency:
  group: ${{ github.workflow }} @ ${{ github.event.pull_request.head.label || github.head_ref || github.ref }}
  cancel-in-progress: true

env:
  CID_WORKFLOW: main
  CID_VERSION: v0.5.0
  CID_LOGLEVEL: ${{ github.event.inputs.loglevel || 'info' }}
  # allowed modes are 'block' and 'audit'. Using https://github.com/step-security/harden-runner to harden the runner.
  EGRESS_POLICY: block

# jobs
jobs:
  # info
  info:
    name: Info
    runs-on: ubuntu-24.04 # https://github.com/actions/runner-images
    timeout-minutes: 10
    if: ${{ github.event.inputs.loglevel == 'debug' }}
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@c6295a65d1254861815972266d5933fd6e532bdf # v2.11.1
        with:
          disable-telemetry: true
          disable-sudo: true
          egress-policy: ${{ env.EGRESS_POLICY }}
          allowed-endpoints: ${{ env.EGRESS_POLICY_ALLOWED_ENDPOINTS }}
      - name: Setup Tools
        uses: cidverse/ghact-cid-setup@c6dac0517d28bd8871c195fee9a6bd5a5854d5cb # v0.2.0
        with:
          version: ${{ env.CID_VERSION }}
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Info
        env:
          CID_LOGLEVEL: ${{ env.CID_LOGLEVEL }}
        run: |
          echo "> project modules"
          cid --log-level=${CID_LOGLEVEL:-info} module ls
          echo "> catalog"
          cid --log-level=${CID_LOGLEVEL:-info} catalog list
          echo "> workflows"
          cid --log-level=${CID_LOGLEVEL:-info} workflow ls
  # go-build - github.com/cidverse/test
  go-build-github-com-cidverse-test:
    name: go-build - github.com/cidverse/test
    runs-on: ubuntu-24.04 # https://github.com/actions/runner-images
    permissions:
      id-token: write # provenance signing
    timeout-minutes: 10
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@c6295a65d1254861815972266d5933fd6e532bdf # v2.11.1
        with:
          disable-telemetry: true
          disable-sudo: true
          egress-policy: ${{ env.EGRESS_POLICY }}
          allowed-endpoints: >-
            proxy.golang.org:443
            storage.googleapis.com:443
            sum.golang.org:443
      - name: Setup Tools
        uses: cidverse/ghact-cid-setup@c6dac0517d28bd8871c195fee9a6bd5a5854d5cb # v0.2.0
        with:
          version: ${{ env.CID_VERSION }}
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: go-build - github.com/cidverse/test
        env:
          CID_WORKFLOW: ${{ env.CID_WORKFLOW }}
          CID_LOGLEVEL: ${{ env.CID_LOGLEVEL }}
        run: |
          cid --log-level=${CID_LOGLEVEL:-info} plan execute -s build --step 0
      - name: Upload Outputs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: "go-build-github-com-cidverse-test-${{ github.run_id }}"
          path: .dist
          retention-days: 1
          if-no-files-found: ignore
  # go-test - github.com/cidverse/test
  go-test-github-com-cidverse-test:
    name: go-test - github.com/cidverse/test
    runs-on: ubuntu-24.04 # https://github.com/actions/runner-images
    permissions:
      id-token: write # provenance signing
    timeout-minutes: 10
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@c6295a65d1254861815972266d5933fd6e532bdf # v2.11.1
        with:
          disable-telemetry: true
          disable-sudo: true
          egress-policy: ${{ env.EGRESS_POLICY }}
          allowed-endpoints: >-
            proxy.golang.org:443
            storage.googleapis.com:443
            sum.golang.org:443
      - name: Setup Tools
        uses: cidverse/ghact-cid-setup@c6dac0517d28bd8871c195fee9a6bd5a5854d5cb # v0.2.0
        with:
          version: ${{ env.CID_VERSION }}
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: go-test - github.com/cidverse/test
        env:
          CID_WORKFLOW: ${{ env.CID_WORKFLOW }}
          CID_LOGLEVEL: ${{ env.CID_LOGLEVEL }}
        run: |
          cid --log-level=${CID_LOGLEVEL:-info} plan execute -s build --step 1
      - name: Upload Outputs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: "go-test-github-com-cidverse-test-${{ github.run_id }}"
          path: .dist
          retention-days: 1
          if-no-files-found: ignore
  # gitleaks-scan
  gitleaks-scan:
    name: gitleaks-scan
    runs-on: ubuntu-24.04 # https://github.com/actions/runner-images
    permissions:
      id-token: write # provenance signing
    timeout-minutes: 10
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@c6295a65d1254861815972266d5933fd6e532bdf # v2.11.1
        with:
          disable-telemetry: true
          disable-sudo: true
          egress-policy: ${{ env.EGRESS_POLICY }}
      - name: Setup Tools
        uses: cidverse/ghact-cid-setup@c6dac0517d28bd8871c195fee9a6bd5a5854d5cb # v0.2.0
        with:
          version: ${{ env.CID_VERSION }}
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: gitleaks-scan
        env:
          CID_WORKFLOW: ${{ env.CID_WORKFLOW }}
          CID_LOGLEVEL: ${{ env.CID_LOGLEVEL }}
        run: |
          cid --log-level=${CID_LOGLEVEL:-info} plan execute -s build --step 2
      - name: Upload Outputs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: "gitleaks-scan-${{ github.run_id }}"
          path: .dist
          retention-days: 1
          if-no-files-found: ignore
  # semgrep-scan
  semgrep-scan:
    name: semgrep-scan
    runs-on: ubuntu-24.04 # https://github.com/actions/runner-images
    permissions:
      id-token: write # provenance signing
    timeout-minutes: 10
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@c6295a65d1254861815972266d5933fd6e532bdf # v2.11.1
        with:
          disable-telemetry: true
          disable-sudo: true
          egress-policy: ${{ env.EGRESS_POLICY }}
          allowed-endpoints: >-
            semgrep.dev:443
      - name: Setup Tools
        uses: cidverse/ghact-cid-setup@c6dac0517d28bd8871c195fee9a6bd5a5854d5cb # v0.2.0
        with:
          version: ${{ env.CID_VERSION }}
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: semgrep-scan
        env:
          CID_WORKFLOW: ${{ env.CID_WORKFLOW }}
          CID_LOGLEVEL: ${{ env.CID_LOGLEVEL }}
        run: |
          cid --log-level=${CID_LOGLEVEL:-info} plan execute -s build --step 3
      - name: Upload Outputs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: "semgrep-scan-${{ github.run_id }}"
          path: .dist
          retention-days: 1
          if-no-files-found: ignore
  # zizmor-scan
  zizmor-scan:
    name: zizmor-scan
    runs-on: ubuntu-24.04 # https://github.com/actions/runner-images
    permissions:
      id-token: write # provenance signing
    timeout-minutes: 10
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@c6295a65d1254861815972266d5933fd6e532bdf # v2.11.1
        with:
          disable-telemetry: true
          disable-sudo: true
          egress-policy: ${{ env.EGRESS_POLICY }}
      - name: Setup Tools
        uses: cidverse/ghact-cid-setup@c6dac0517d28bd8871c195fee9a6bd5a5854d5cb # v0.2.0
        with:
          version: ${{ env.CID_VERSION }}
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: zizmor-scan
        env:
          CID_WORKFLOW: ${{ env.CID_WORKFLOW }}
          CID_LOGLEVEL: ${{ env.CID_LOGLEVEL }}
        run: |
          cid --log-level=${CID_LOGLEVEL:-info} plan execute -s build --step 4
      - name: Upload Outputs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: "zizmor-scan-${{ github.run_id }}"
          path: .dist
          retention-days: 1
          if-no-files-found: ignore
  # github-sarif-upload
  github-sarif-upload:
    name: github-sarif-upload
    runs-on: ubuntu-24.04 # https://github.com/actions/runner-images
    needs: [gitleaks-scan, semgrep-scan, zizmor-scan]
    permissions:
      id-token: write # provenance signing
    timeout-minutes: 10
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@c6295a65d1254861815972266d5933fd6e532bdf # v2.11.1
        with:
          disable-telemetry: true
          disable-sudo: true
          egress-policy: ${{ env.EGRESS_POLICY }}
      - name: Setup Tools
        uses: cidverse/ghact-cid-setup@c6dac0517d28bd8871c195fee9a6bd5a5854d5cb # v0.2.0
        with:
          version: ${{ env.CID_VERSION }}
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: github-sarif-upload
        env:
          CID_WORKFLOW: ${{ env.CID_WORKFLOW }}
          CID_LOGLEVEL: ${{ env.CID_LOGLEVEL }}
        run: |
          cid --log-level=${CID_LOGLEVEL:-info} plan execute -s build --step 5
      - name: Upload Outputs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: "github-sarif-upload-${{ github.run_id }}"
          path: .dist
          retention-days: 1
          if-no-files-found: ignore
